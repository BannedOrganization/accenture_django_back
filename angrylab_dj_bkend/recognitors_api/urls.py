from django.urls import path
from . import views


urlpatterns = [
    path('car_number', ..., name='car_namber'), # get, post
    path('tomato', ..., name='car_namber'), # get, post
    path('buckwheat', ..., name='car_namber'), # get, post
]
